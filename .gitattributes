# Text files normalise to LF in the repo and to the platform ending on checkout.
* text=auto

# The VBE expects CRLF in imported VBA source, so the .bas blobs are stored
# CRLF and shipped byte-for-byte — a Raw download and a repo ZIP both import
# the same way the working tree does.
*.bas -text whitespace=cr-at-eol
